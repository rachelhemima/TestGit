git clone https://github.com/rachelhemima/bootjpa.git
git commit -m 'adding new files' 
git push origin master
cd bootjpa

git config --global user.name <name>
git config --global user.email <name>


